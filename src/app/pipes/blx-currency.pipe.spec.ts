import { BlxCurrencyPipe } from './blx-currency.pipe';

describe('BlxCurrencyPipe', () => {
  it('create an instance', () => {
    // const pipe = new BlxCurrencyPipe();
    // expect(pipe).toBeTruthy();
  });
});
